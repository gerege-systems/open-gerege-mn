<!--
  Gerege Template Platform V3.0
  Gerege Systems Development Team болон Claude AI хамтран бүтээв, 2026.

  Ганц цонх — нэвтрэлтийн төлвөөс хамаарч Login эсвэл Dashboard хуудсыг агуулна.
-->
<Window
    x:Class="GeregeDesktop.Client.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Gerege Template">

    <Grid x:Name="RootGrid" Background="{ThemeResource BgBrush}">

        <!-- Хадгалсан session-ыг шалгах хугацаанд -->
        <StackPanel
            x:Name="SplashPanel"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="12">
            <ProgressRing IsActive="True" Width="32" Height="32" />
            <TextBlock
                Text="Session шалгаж байна…"
                Style="{StaticResource BodyText}"
                HorizontalAlignment="Center" />
        </StackPanel>

        <!-- Идэвхтэй хуудас энд солигдоно -->
        <ContentControl
            x:Name="PageHost"
            Visibility="Collapsed"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch" />

    </Grid>

</Window>
